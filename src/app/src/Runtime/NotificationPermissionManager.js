import NotificationPermissionManager from 'shared/Runtime/NotificationPermissionManager'
import RuntimePaths from './RuntimePaths'

export default new NotificationPermissionManager(RuntimePaths.NOTIFICATION_PERMISSION_PATH)
